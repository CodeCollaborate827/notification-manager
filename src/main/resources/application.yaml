spring:
  application:
    name: notification-manager
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: notification_manager
  cloud:
    function:
      definition: messageMentionedDownstreamConsumer;messageReactedDownstreamConsumer;newFriendRequestDownstreamConsumer;newNotificationUpstream; func1
    Stream:
      bindings:
        messageMentionedDownstreamConsumer-in-0:
          destination: topic.conversation.notification
        messageReactedDownstreamConsumer-in-0:
          destination: topic.conversation.notification
        newFriendRequestDownstreamConsumer-in-0:
          destination: topic.user.notification
        newNotificationUpstream-out-0:
          destination: topic.user.notification
        func1-in-0:
          destination: topic-input
        func1-out-0:
          destination: topic-output
      kafka:
        binder:
          brokers: localhost:9092
server:
  port: 8084
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
